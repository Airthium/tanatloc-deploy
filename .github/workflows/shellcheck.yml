name: Shellcheck
on: push

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: un Shellcheck
        uses: ludeeus/action-shellcheck@master
